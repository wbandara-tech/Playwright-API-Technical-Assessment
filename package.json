{
  "name": "playwright-api-technical-assessment",
  "version": "1.0.0",
  "description": "Playwright API automation test suite for restful-api.dev",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.2"
  }
}
